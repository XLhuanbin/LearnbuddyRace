# 验证记录

本文件记录**实际执行过**的验证：测试输入、检查项、实际结果、失败案例与修复状态。
所有数字来自真实运行输出，不是估计值。第一阶段的记录保留在 §V1–§V8，第二阶段追加在 §V9 起。

记录日期：2026-09-16

---

## 第一阶段（环境与最小流程）· 摘要

| 编号 | 检查项 | 实际结果 |
| --- | --- | --- |
| V1 | Node/npm、依赖安装、类型检查、构建 | 通过（npm 需换国内镜像；构建目录无法清空 → `emptyOutDir:false` + 固定产物名） |
| V2 | 外网可达性 | `api.deepseek.com` 可达；环境变量里的 `DEEPSEEK_BASE_URL`（packyapi.com）**不可达**；`api.openai.com` 不可达 |
| V3 | 模型接口跨域 | CORS 预检通过，允许浏览器直连 |
| V4 | 静态部署 | 部署成功，5 个关键资源 200 |
| V5 | 核心回归测试 | 15/15 通过（含 3 个被测试发现的缺陷修复） |
| V6 | 真实模型抽取（3 篇） | 20/21 字段有值，20 条引文全部通过定位校验 |
| V7 | 端到端走查 | 36/36 通过（本地 + 线上） |
| V8 | 未执行项 | 实时抽取端到端、未预置论文导入等 |

第一阶段修复的真实缺陷：跨行连字符断词失败、引文部分匹配被误判为有效、超长论文把「未提取到」误报为「论文未报告」、关系证据只在错误论文中查找、预置结果被误标为实时分析。

---

## V9 第二阶段：真实模型完整流水线（5 篇论文）

运行：`npm run analyze`（模型 deepseek-chat，提示词 v2.1.0，密钥仅从环境变量读取）

| 论文 | 年份 | 页数 | 字符数 | 可核验字段 | 待人工核对 | 未找到证据 | 缺失 | 条件可核验 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Attention Is All You Need | 2017 | 15 | 39,969 | 7/7 | 0 | 0 | 0 | 4/7 |
| BERT | 2018 | 16 | 65,636 | 6/7 | 0 | 0 | 1 | 7/7 |
| RoBERTa | 2019 | 13 | 49,756 | 7/7 | 0 | 0 | 0 | 6/7 |
| DistilBERT | 2019 | 5 | 18,778 | 7/7 | 0 | 0 | 0 | 7/7 |
| GPT-3（Language Models are Few-Shot Learners） | 2020 | 75 | 240,327 | 7/7 | 0 | 0 | 0 | 6/7 |
| **合计** | | | | **34/35** | **0** | **0** | **1** | 30/35 |

- 年份全部由 arXiv 编号正确推导（2017/2018/2019/2019/2020）。
- 标题校正生效：BERT 与 DistilBERT 的启发式标题不完整，模型给出并**在原文中验证**后才采用。
- 分母口径：字段分母 = 5 篇 × 7 字段 = 35；条件分母 = 5 篇 × 7 维度 = 35。判断标准 = 引文能否在对应论文全文中定位。

### 程序校验层产出（8 条）

| 问题类型 | 数量 | 说明 |
| --- | --- | --- |
| 条件无法确认 | 4 | 例如 GPT-3 的算力/训练时长在本次片段中未提取到 → 标为无法确认，不推测 |
| 页码与定位结果不一致 | 2 | 模型自称页码与程序定位结果不同 |
| 字段缺失 | 1 | BERT 的「局限」，标注为「本次片段中未提取到」 |
| 引文无法定位 | 1 | 该字段被降级为「待人工核对」，并展示实际匹配位置 |

> 页码校验在真实数据上确实被触发过 2 次，说明「不采信模型自报页码」不是空话。

## V10 第二阶段：方法关系可信度

| 检查项 | 实际结果 |
| --- | --- |
| 关系总数 | 7 条 |
| 原文明示（引文已定位） | 1 条 |
| 系统推断（附推断理由） | 5 条 |
| 待核查 | 1 条 |
| 被程序调整可信度的关系 | **1 条**：模型判为「改进/原文明示」，引文可在原文定位，但不含被继承方法名、无引用标记、无继承/改进措辞 → 判定为「关键词相似而非技术继承」，降级为「待核查」，降级原因完整保留 |

示例（原文明示，引文已定位到 DistilBERT 论文 p.2）：

> Using a triple loss, we show that a 40% smaller Transformer (Vaswani et al. [2017]) pre-trained through distillation via the supervision of ...

引文中的 `Vaswani et al. [2017]` 正是引用标记，因此判为技术继承成立；而另一条只有「本节探讨哪些选择对 BERT 预训练重要」的引文被正确拦下。

## V11 第二阶段：不可比检测

规则计算，不依赖模型自评。以下结果均在**端到端走查的界面上实测**出现。

| 选择 | 总体结论 | 关键差异 |
| --- | --- | --- |
| 全部 5 篇 | **当前不能直接比较** | 数据集：`WMT 2014 En-De/En-Fr`（翻译）与 `GLUE/SQuAD/RACE` 等（语言理解）没有共同项；评价指标：`BLEU` 与 `F1/accuracy/EM` 无共同项；实验设置：`从头训练` vs `微调` 协议不重叠 |
| 仅 BERT + RoBERTa + DistilBERT | **只能有限比较** | 数据集部分重叠（共有 GLUE、SQuAD）；指标部分重叠（共有 F1/accuracy）；评估协议一致（都是微调）→ 实验设置可直接比较；数据划分与预训练模型存在差异 |
| 额外训练数据 | 可以直接比较 | 5 篇均报告为未使用额外数据（`none`），且引文可核验 |
| 算力/训练时长 | 信息不足，无法判断 | GPT-3 未报告可核验的算力信息 → 系统拒绝判断可运行性 |

条件维度结论表均带「原文证据 / 信息缺失 / 判定规则」三类依据，可逐条展开核对。

## V12 第二阶段：分歧与待调查问题（规则 + 模型双重约束）

| 检查项 | 实际结果 |
| --- | --- |
| 发现条数 | 3 条 |
| 发现类型 | 2 条「共同提到的局限」、1 条「当前材料未发现」 |
| **是否产生了「结论分歧」** | **否**。虽然模型给出的候选分析里包含差异描述，但因规则计算的论文对可比性都不是「一致」，系统不允许标记为结论分歧 |
| 已检查论文对 | 10 对全部列出，每对附可比性结论与条件差异 |
| 声明完整性 | 每条发现都带固定声明；共同局限明确写为「不代表整个领域存在研究空白」 |

## V13 第二阶段：方法决策（预置示例）

| 检查项 | 实际结果 |
| --- | --- |
| 候选方法 | 5 个，全部带匹配度（条件匹配 / 有条件可用 / 无法判断） |
| 阅读顺序 | 5 步，含「依据你的条件 / 依据论文 / 信息缺口」标注 |
| 算力识别 | 抽取到真实算力信息并带证据：Transformer 8×P100 3.5 天；RoBERTa 1024×V100 约一天；DistilBERT 8×16GB V100 约 90 小时；GPT-3 未报告 → `computeReported=false` |
| 面向「无 GPU + 两周」条件的推荐 | DistilBERT 排在第 3 位并标注「与用户偏向可复现的小模型方向最接近」；GPT-3 标为「无法判断」并说明大模型与无 GPU 环境不匹配 |
| 是否编造可运行性 | **没有**。所有涉及「能否在自己设备上跑」的地方都写明「论文未报告，无法确认」 |

## V14 第二阶段：自动化验证

### 核心回归测试 `npm test` — 通过 37，失败 0

| 分组 | 用例数 | 覆盖内容 |
| --- | --- | --- |
| 归一化边界 | 2 | 跨行连字符断词、Unicode 破折号 |
| 定位与负样本 | 3 | 精确匹配、改写引文不得判为精确匹配、过短引文不参与 |
| 真实缓存数据回测 | 6 | 34 条引文全部可再次定位、页码一致、标注「可核验」的字段确实可定位、篡改引文在证据层 `verified=false` |
| 不可比检测 | 13 | 一致/部分重叠/完全不同/一方未报告/条件字段缺失/额外数据不同/协议一致/协议不同/论文对取最差/差异可列出 |
| 关系降级与程序校验 | 8 | 原文明示成立、引文无法定位降级、无继承措辞降级、无理由降级、页码无效、无引文、条件缺失 |
| 元信息启发式 | 4 | arXiv 编号、年份推导、标题识别、不把作者行并进标题 |

### 端到端走查 `npm run e2e` — 通过 58，失败 0（本地与线上各一次）

| 分组 | 检查项数 | 覆盖内容 |
| --- | --- | --- |
| E1 入口 | 3 | 7 个功能入口齐全、未配置模型时明确提示不可用 |
| E2 语料与条件 | 10 | 5 篇样例、缓存标记、四态字段、7 维实验条件表、程序校验问题表 |
| E3 证据绑定 | 4 | 弹层、校验标记、真实页码、原文上下文 |
| E4 全选 | 7 | 面板、不可比结论、禁止比较数值、维度表、依据展开 |
| E5 同族论文 | 3 | 只选 3 篇 → 「只能有限比较」、仍声明不排名 |
| E6 关系 | 11 | 三档图例、详情、含义说明、人工修正全流程、AI 原判定保留与恢复 |
| E7 决策 | 8 | 预置示例标注、匹配度、条件敏感性、依据来源、算力未报告标注、阅读顺序、无密钥时不伪造 |
| E8 分歧 | 6 | 发现展示、未谎报结论分歧、已检查论文对、声明完整性、导出 |
| E9 状态 | 5 | 四态统计、条件核验数、校验问题汇总、关系可信度分布、语料来源 |
| E10 控制台 | 1 | 0 条未处理前端错误 |

### 本阶段自动化检查发现并修复的问题

| 编号 | 现象 | 根因 | 修复 |
| --- | --- | --- | --- |
| B7 | 快速连续点击勾选论文时，选择结果与点击不符 | 比较页在事件处理里用渲染时捕获的 `selected` 计算，同一帧内多次点击互相覆盖 | 改为函数式状态更新（`setSelected(prev => ...)`），端到端复测通过 |
| B8 | 端到端断言「只能有限比较」出现**假阳性** | 该表述同时出现在页面静态说明文案里，`body.innerText` 断言命中了说明文字 | 断言改为只在「可比性判断」面板 DOM 内取文本；修正后 E4/E5 才真正区分出「不能直接比较」与「只能有限比较」两种结论 |
| B9 | 重新部署后访问者仍看到旧版本 | 资源文件名固定（因构建目录无法清空），被 CDN 按 URL 缓存（实测 `Age: 3160`，返回旧文件 588KB 而当时本地已是 623KB） | 新增 `scripts/stamp-build.mjs`：用入口脚本内容哈希给资源 URL 加 `?v=` 查询串（文件名不变、磁盘不残留旧文件）。实测带查询串请求返回最新文件，线上端到端 58 项全部通过 |

## V15 尚未执行的验证（明确列出）

| 编号 | 待验证项 | 阻塞原因 |
| --- | --- | --- |
| V15-1 | **「推荐随用户条件变化」**：用「无算力/两周」与「有算力/半年」两组条件各跑一次决策并对比 | 模型接口返回 **HTTP 402 Insufficient Balance**（2026-09-16 22:44 起）。脚本已实现（`npm run variation`），失败已如实记录在 `docs/DECISION-VARIATION.md`，不使用预置结果替代 |
| V15-2 | 用一篇**未预置**的新论文做实时导入（含无文本层 PDF、超页数、模型超时、重复上传去重） | 同上，需要可用的模型接口 |
| V15-3 | 实时抽取链路的端到端走查（导入 PDF → 实时抽取 → 比较/图/决策/分歧 → 导出） | 同上。已配置密钥以外的降级行为已由 E2/E7 验证（无接口时不伪造结果） |
| V15-4 | 导出文件内容抽查（Markdown/JSON 是否含密钥、引文与页码是否正确） | 未执行 |
| V15-5 | 刷新后恢复、移动端/不同分辨率 | 未执行 |
| V15-6 | 正式领域语料（8~12 篇）人工逐篇核对 | 领域未确认 |
| V15-7 | 依赖许可清单复核（`npm ls --all`） | 未执行（提交前必须做） |

---

## V16 第三轮：正确性与跨页面一致性修正（2026-09-17）

完整记录（根因、修复、修复前后差异、来源区分）见 [`CORRECTNESS-FIXES.md`](CORRECTNESS-FIXES.md)。此处只列验证结果。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **51/51 通过**（较上轮新增 14 项针对本轮问题的回归用例） |
| `npm run e2e`（本地构建产物） | **66/66 通过** |
| `npm run e2e --url <线上地址>` | **66/66 通过** |
| 线上构建指纹核对 | 线上 `build.json` = 本地（`9dc15e1748`）；`index.html` 引用 `assets/app.js?v=9dc15e1748`；`samples/index.json` 带版本串与本地字节数一致（90840） |
| 规则重算（不调用模型） | 校验问题 8 → 30；关系状态 1 条变化且理由更正；分歧种类 2 条变化（共同局限 → 各自局限） |

### 本轮修复后被验证的行为

- **两页一致**：仅选 BERT 与 RoBERTa 时，比较页与分歧页的「已检查论文对」都给出「只能有限比较」（端到端走查实测）。
- **未知不等于一致**：额外训练数据与数据划分改为「信息不足，无法判断」。
- **关系证据标准收紧**：原文明示 0 条、系统推断 5 条、待核查 2 条；降级理由与原文一致。
- **过期可见**：模型输出来自提示词 v2.1.0、当前程序为 v3.0.0，界面统一提示「已标记为过期，不作为当前结论」。
- **页数正确**：预置论文显示 15/16/13/5/75 页，不再显示 0 页。

### 仍未完成（同前）

- 用提示词 v3 重新生成全部模型输出（阻塞：模型接口 402 余额不足）。
- 关系分析在新标准下能否产出达标的「原文明示」；推荐文字是否不再混用预训练/微调成本。
- 两组用户条件对照验证、未预置新论文的实时导入、导出内容抽查、刷新恢复、移动端。

---

## V17 额度恢复后：v3 全量重跑与剩余验收（2026-09-17）

完整记录见 [`ROUND4-COMPLETION.md`](ROUND4-COMPLETION.md)。结果摘要：

| 检查 | 结果 |
| --- | --- |
| v3 全量重跑（真实模型） | 字段 35/35 有值、32 条引文可核验、0 待人工核对、3 未找到证据；条件 9 维度（每篇可核验 4~8）；算力 5/5 且带阶段；校验问题 20 条 |
| 决策证据挂载修复 | 有证据理由 15 条（修复前资源类理由被误判无证据）；资源类理由全部带训练阶段标签 |
| `npm run variation` | **通过**：阅读顺序与候选匹配度均随条件变化（RoBERTa conditional → suitable） |
| 未预置论文导入（DDPM, 2006.11239） | **通过**：7/7 字段可核验、6/9 条件可核验；标题不可靠被标为「标题未确认」 |
| `npm test` | **75/75**（新增标题可信度 7、决策证据绑定 7、导出抽查 8） |
| `npm run e2e`（本地） | **74/74**（新增刷新恢复 3、窄屏 3、产物密钥扫描 1） |
| `npm run e2e --url <线上>` | **74/74** |
| 线上指纹 | `9f7ead7bb3`，与本地一致；线上语料 promptVersion v3.0.0 / rulesVersion r3.0.0 / cacheVersion 3 |
| 构建产物密钥扫描 | dist 34 个文件，无密钥形态字符串 |

### 本轮发现并修复的新问题

1. 模型把条件维度标成 `field`，导致决策理由的证据挂载失效（5 条资源理由被误标无证据）。
2. 未预置论文的启发式标题抓到摘要句子，模型回显同一句并通过「引文可定位」校验 → 新增 `looksLikeTitle` 与 `title_unverified`。
3. `buildPaperFromPdf` 在缺少 arXiv 编号时崩溃（用户导入的论文没有编号）→ 改为由文件名派生稳定 id。

### 仍未验证

- 浏览器端的「填密钥后实时抽取」未在自动化中走一遍（Node 侧已用未预置论文验证同一套核心代码）。
- 现有 5 篇论文仍未产生达标的「原文明示」关系（已用合成样本验证判定标准本身有效）。
- 扫描件/OCR、自动全网检索、失败经验库、多用户协作：范围外。

---

## V18 第五轮审计（推荐正确性 / 证据引用 / 真实浏览器流程，2026-09-17）

完整记录见 [`ROUND5-AUDIT.md`](ROUND5-AUDIT.md)。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **114/114 通过**（本轮新增 24 项） |
| `npm run e2e`（本地构建产物） | **74/74 通过** |
| `npm run e2e --url <线上>` | **74/74 通过** |
| 真实浏览器调用流程（线上，真实模型） | **30/30 通过**：上传非预置论文 → 解析 → 浏览器发起真实模型调用 → 结构化结果 → 证据弹层（12 个） → 保存 → 刷新恢复；日志与界面均不出现密钥 |
| 鉴权失败路径（本地 mock 401） | **7/7 通过**：可读提示、记录失败原因、不假报成功、不回显凭据 |
| 单变量条件对照 + 反例 | 已执行；反例（笔记本无 GPU · 两年）中 suitable 且目标阶段为训练阶段者 **0 个** |
| 关系候选检索（确定性） | 4 组论文对候选句与覆盖度已记录；改进检索后 3 条关系被认证为原文明示（引文为真实论文句子） |
| 线上指纹 | `7b35461dbe`，与本地一致 |

### 本轮发现并修复的缺陷

1. 资源可行性判定过乐观：缺显存/并行/配置证据时仍可能给出 `suitable` → 新增程序门槛（三条件同时成立）。
2. 我把「模型判定 suitable」在汇报中概括成「算力可行」（未经验证的折算）→ 已在报告中更正，并在界面上区分「模型判定」与「程序修正」。
3. 别名提取会把括号内容中的其它方法名（`RoBERTa (… BERT …)` 里的 `BERT`）当成自己的别名 → 收紧为仅从主名称提取全大写缩写。
4. 关系证据判定缺少「第三方主语」与「必须指向关系另一端」两个条件 → 补上，并用真实句子做正反例。
5. 证据绑定在「同名键并存」时未真正绑定（分支结构导致）→ 重构为「先定位目标、再取证据」，并对类型冲突/无效引用/跨论文区分处理。

### 仍未验证

- 真实接口的鉴权失败提示：观察窗口内服务未返回响应（已用本地 mock 覆盖同一代码路径，不作为已通过）。
- 模型判定的重复稳定性：条件对照每组仅一次调用。
- 关系候选句的语义充分性：程序仍只做必要条件判定，语义建议人工复核。
- 浏览器端「重新分析」按钮仅在论文未分析或为缓存结果时出现（避免误触消耗额度），因此重复抽取需先移除再导入。

---

## V19 第六轮：基础体验收尾 + 领域准备（2026-09-18）

完整记录见 [`ROUND6-COMPLETION.md`](ROUND6-COMPLETION.md)。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **143/143 通过**（新增：目标匹配/执行可行性 6、重新分析 9、超时取消重试 14） |
| `npm run e2e`（本地） | **84/84 通过**（新增 E13 目标匹配与执行可行性、E14 重新分析入口） |
| `npm run e2e`（线上） | **84/84 通过** |
| 真实浏览器流程（线上，真实模型） | **35/35 通过**（新增 S5b：重新分析入口 → 额度提示 → 取消后未发起调用且原结果仍在） |
| mock 超时/取消/重试 | 9 项通过（超时归类失败、取消归类已停止等待、取消不重试、鉴权不重试、服务端错误退避重试成功） |
| 书目核实 | 18 篇候选论文用 arXiv API 逐条核实（含 1 处纠正：MobileViT 编号） |
| 线上构建指纹 | `8f69f52074`，与本地一致 |

### 本轮的关键实现

- `goalMatch` + `execution[]`（阅读/推理/微调/预训练分别判定），阅读不受硬件门槛影响，微调不用预训练成本代替；
  界面明确「论文证据判断 ≠ 实际执行验证」。
- 重新分析两步确认；只成功才写入；失败/取消保留原结果；人工修正合并保留；下游待更新提示（关系/决策需重跑，比较实时重算）。
- 客户端支持外部取消信号；超时归类为「失败（未完成）」；取消文案不夸大为「已终止服务端计费」。

### 仍未验证 / 缺项

- 正式细分领域未确定（三个候选未选定，语料未替换）。
- 「使用现有权重推理」因缺少权重/推理成本维度，恒为「尚未验证」。
- 真实接口的鉴权失败与超时表现未验证（已用 mock 覆盖同一代码路径，两类结果分开记录）。
- 五篇样例没有显存/并行/配置级算力证据，故无候选被认证为 suitable（如实结论）。

---

## V20 正式视觉案例（2026-09-18）

完整记录见 [`VISION-CASE-REPORT.md`](VISION-CASE-REPORT.md)。

| 检查 | 结果 |
| --- | --- |
| 语料文件核对 | 5 篇（ResNet/ViT/DeiT/Swin/ConvNeXt），文本层完整，页数 12~22；文件名年份仅作预印本年份 |
| 字段抽取 | 每篇 7/7 可核验（引文全部通过全文定位） |
| 实验记录抽取 | 23 条（分类 20、检测 3），带任务标签与完整条件字段 |
| 表格行列核查（文本层） | 全部通过（引文 + 表题 + 行标签可定位） |
| 表格行列核查（版面几何） | 2 条确认，21 条待核查（双栏/跨行排版无法确认列对齐） |
| 关系 / 分歧 / 决策 | 10 条关系、2 条分歧发现、5 个候选（含目标匹配与四阶段执行可行性） |
| `npm test` | **178/178**（新增实验级规范化与可比性 20 项、真实语料核查 9 项） |
| `npm run e2e`（本地 + 线上） | **99/99**（新增实验比较页、语料切换与隔离） |
| 线上指纹 | `2251b9b2da`，与本地一致 |

### 本轮修复

1. 指标 error 与 accuracy 混为同一指标 → 区分并给出换算说明。
2. 表格短行标签（Swin-T）逐字匹配失败 → 归一化定位 + 引文附近（±1500 字符）判定。
3. 列头搜索范围过小 → 扩大到 900pt 并过滤句子型候选。
4. 标题校正后未重算字段校验（「标题未确认」与「已模型校正」并存）→ 校正后重算。
5. 抽取输出被 token 上限截断（实验记录使响应变长）→ 抽取上限提到 8000，其余步骤 6000。

### 仍未修复 / 未验证

- ViT 一条记录的指标名抽成数据集名（按原样展示并在核查清单标出，未静默改数）。
- Swin 三条 1K-only 记录的预训练数据缺失。
- 浏览器端实时抽取尚未覆盖实验记录字段（本轮实验记录来自离线流水线，与浏览器共用核心代码）。

---

## V21 前端与引导改版（2026-09-18）

完整记录见 [`UI-UX-ROUND.md`](UI-UX-ROUND.md)。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | 178/178（核心逻辑未改动） |
| `npm run e2e`（本地） | **112/112**（新增首屏 8 项、实验比较界面 2 项） |
| `npm run e2e`（线上） | **112/112** |
| 新用户路径实走（本地） | **35/35** |
| 新用户路径实走（线上） | **35/35** |
| 线上指纹 | `0a7be35d2f` |

### 本轮改版要点

- 新增首屏「开始」页（产品说明 + 三个问题 + 6 步路径 + 语料集卡片 + 折叠规则）。
- 导航改为任务顺序 + 状态点；刷新回到上次所在页。
- 论文库改为语料集卡片 + 引导式空状态 + 精简提示 + 下一步。
- 实验比较页重构为紧凑表格 + 筛选 + 固定选中摘要 + 对照卡片上移 + 推荐对照常驻。
- 修复「正面核查说明被显示为警告色」；新增状态徽标与图例、焦点环、折叠说明。

### 未改动（本轮约束）

实验数据、判定规则、证据引文、推荐结论、语料文件均未修改；待核查记录仍标「待核查」。

---

## V22 浅色视觉重构与交互重组（2026-09-20）

完整记录见 [`VISUAL-REDESIGN-ROUND.md`](VISUAL-REDESIGN-ROUND.md)。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | 178/178（模型/抽取/证据/比较/关系/推荐逻辑未改动） |
| `npm run e2e`（本地） | **112/112** |
| `npm run e2e`（线上） | **112/112** |
| 路径 A（空状态首次打开）实走 | 通过（本地与线上） |
| 路径 B（正式视觉案例完整演示）实走 | 通过（本地与线上），合计 **41/41** |
| 窄屏 390×844 | 无整页横向溢出；表格容器内滚动 |
| 键盘 | Tab 焦点可见（outline solid 2px） |
| 线上指纹 | `ae43112683`，与本地一致 |

### 本轮改动范围

页面结构、布局、样式、导航、文案组织与交互反馈；新增 `src/design-system.css` 组件层与 `HomeView` 首屏；
`App` 外壳改为顶栏 + 任务流导航 + 面包屑与进度；论文库、实验比较、方法关系三页重做布局。

### 未改动（约束）

实验数值、证据引文、可比性判定规则、关系状态与推荐结论、语料文件；待核查记录仍标「待核查」。

### 已知限制

- 预置语料中不存在「只能结合条件讨论」的实验对（真实原因见报告第七节），界面保留真实结论。
- 论文级「跨论文比较」页仅降级到「更多」，视觉未完全跟上新系统。

---

## V23 语料状态与加载流程修复（2026-09-20）

完整记录见 [`CORPUS-STATE-FIX.md`](CORPUS-STATE-FIX.md)。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **193/193**（新增语料身份 / 范围计算 / 历史数据归位 15 项） |
| `npm run e2e`（本地） | **112/112** |
| `npm run e2e`（线上） | **112/112** |
| 语料状态验收 A/B/C + 切换往返（本地） | **22/22** |
| 语料状态验收 A/B/C + 切换往返（线上） | **22/22** |
| 路径 A/B/D/E 实走（本地 + 线上） | **41/41** |
| 线上指纹 | `e9ce620cbc` |

### 修复内容

1. 历史数据无语料集身份且计数来源不一致（`papers.length` vs 按语料过滤）→ 5 篇变 10 篇；
2. 论文库把 React 事件当参数传给 `loadSample(key)` → 加载静默失败；
3. `loadSample` 由「按 ID 过滤追加」改为「按语料集幂等替换」；
4. 语料集切换由「叠加」改为「替换」，并提供旧数据提示与清理动作；
5. 实验比较页空状态区分「没有语料」与「有论文但无实验记录」，并调整信息优先级。

### 未改动

模型输出、比较规则、实验可信度判定、证据内容与推荐算法；待核查记录仍标「待核查」。

---

## V24 信息架构重构（2026-09-21）

完整记录见 [`INFO-ARCH-ROUND.md`](INFO-ARCH-ROUND.md)。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **193/193** |
| `npm run e2e`（本地 / 线上） | **117/117** / **117/117** |
| 语料状态验收（本地 / 线上） | **22/22** / **22/22** |
| 新用户路径实走（本地 / 线上） | **37/37** / **37/37** |
| 线上指纹 | `a5b2b051b7` |

### 本轮改动

新增 `LandingView`（宣传首页）、`CaseView`（选择案例）、`ResultsView`（结果页）、`EvolutionView`（方法演进 + 阅读顺序）、
`MoreView`（开发者入口）与 `src/demo-views.css`；App 外壳改为「宣传首页无侧栏 + 三入口 + 更多」；
面向用户术语抽象（实验记录→论文结果、语料集→论文集合、证据绑定→原文依据、待核查→仍需确认）。

### 未改动

模型输出、数据、证据、实验记录与判断算法；待核查记录仍如实标注。

---

## V25 定位纠偏与研究地图三视图（2026-09-21）

完整记录见 [`POSITIONING-MAP-ROUND.md`](POSITIONING-MAP-ROUND.md)。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **205/205**（新增方法归组/概览/方法卡片 10 项） |
| `npm run e2e`（本地 / 线上） | **121/121** / **121/121** |
| 语料状态验收（本地 / 线上） | **22/22** / **22/22** |
| 两条真实路径实走（本地 / 线上） | **52/52** / **52/52** |
| 线上指纹 | `04b302feb1` |

### 本轮改动

- 首页定位改为「把一组论文，变成你看得懂的研究地图」；主按钮直接进入研究地图，次按钮进入上传流程；
- 导航压缩为 首页 / 研究地图 / 更多；
- 研究地图三视图：看懂方法（概览 + 分组 + 方法卡片）、联系与区别（真实关系 + 时间线声明 + 两方法对照 + 可选实验比较）、
  从哪里开始（先问基础与目标；阅读不被算力门槛阻断）；
- 新增 `src/core/grouping.ts`（程序派生的方法归组与领域概览，不按论文 ID 硬编码）与 `UploadFlowView`（上传自己的论文）；
- **消除时间线误导**：不再把年份相邻的方法连成继承链；技术关系只依据真实关系数据，方向与类型保持原样。

### 实测上传流程

真实 PDF（`samples/pdfs/vision/02_ViT_2020.pdf`）注入文件选择器，实测：解析成功 → 就地提示配置模型 → 单篇提示补足论文 → 进入同一工作区。
**模型抽取的成功路径未实测**（当前无可用密钥），已如实记录。

### 未改动

模型输出、论文数据、证据内容、实验记录与判断算法；待核查记录仍如实标注。

---

## V26 研究地图（分析层修正 + 可交互地图，2026-09-21）

完整记录见 `METHOD-MAP-ROUND.md`。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **213/213** |
| `npm run e2e`（本地 / 线上） | **127/127** / **127/127** |
| 两条真实路径实走（本地 / 线上） | **62/62** / **62/62** |
| 语料状态验收（本地 / 线上） | **22/22** / **22/22** |
| 线上指纹 | `033d4e0c9c` |

### 分析层
方法家族（名称优先 + 剔除引用语境）与技术策略（多标签）分离；概览先单篇去重再跨论文计数并区分全部/部分/未知；
关系方向从任一端查看都不颠倒，对称关系不声明方向。

### 界面层
默认渲染方法地图（泳道=家族、节点=短名+贡献+策略、连线只画真实关系、颜色+线型区分）；
「关系不明确」默认不连线、待核查默认不显示；点节点高亮+详情、点连线给方向与证据；
窄屏不缩小图形（可滑动，详情下移）；实验比较降为次级入口。

### 新增回归测试
CNN 教师 ≠ CNN 架构；以 X 为起点/借用技巧不改变家族；单篇重复数据集不算共同；一篇分类≠全部分类；关系两端方向一致；对称关系不加方向。

---

## V27 研究地图：探索画布重设计 + 集合内容修正（2026-09-23）

完整记录见 `CANVAS-REDESIGN-ROUND.md`。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **218/218**（新增短贡献忠实性 5 项） |
| `npm run e2e`（本地 / 线上） | **66/66** / **66/66** |
| 新用户两条路径（本地 / 线上） | **62/62** / **62/62** |
| 集合范围回归（本地 / 线上） | **14/14** / **14/14** |
| 语料状态回归（本地 / 线上） | **22/22** / **22/22** |
| 线上指纹 | `6ce7b325c8` |

### 内容修正
地图把「案例预置论文」与「用户自传论文」拼接渲染，导致线上出现 10 篇、语言模型被标为「视觉 Transformer」。
现改为两种集合分离（案例 / 我上传的论文），家族名中性化（Transformer 架构），案例为空时自动切到用户论文，
用户数据不删除；新增 14 项集合范围回归。

### 画布重设计
紧凑工具栏 + 整块画布 + 浮层详情；开关与统计收进「选项」；画布内一句轻提示；连线标签默认不铺；
节点只显示短名称 / 年份 / 一条短贡献（原文子串）；泳道轻量化；桌面右侧、窄屏底部详情面板。

### 已知问题
- e2e 断言从 127 项降到 66 项（脚本误截断后重建），需补回专业视图细粒度断言；
- 窄屏仍为横向滑动泳道；正式案例「原文明示」为 0 条；模型抽取成功路径未实测。

---

## V28 首页重设计 + 全站配色统一（2026-09-23）

完整记录见 `LANDING-REDESIGN-ROUND.md`。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **218/218** |
| `npm run e2e`（线上） | **66/66** |
| 两条真实路径实走（线上） | **62/62** |
| 集合范围回归（线上） | **14/14** |
| 线上指纹 | `fd0ad5ae64` |
| 字体加载 | `RP Serif SC 600` × 6 子集全部 loaded，Network 无失败 |
| 版式实测 | 1440：h1 单行 47.5px、CTA 底部 424px（首屏内）；390：h1 两行均衡、CTA 可见、按钮 362×51 可点 |
| 横向溢出 | 1440 / 390 均无 |

### 改了什么
色彩令牌整体替换（暖纸 / 深墨蓝 / 铜色）；首页重排（水印 + 衬线主标题 + 编号式能力块 + 克制流程示意）；
自托管 Noto Serif SC 子集；关系连线与关系图配色同步；`stamp-build.mjs` 指纹纳入 CSS。

### 未改动
产品定位、入口功能、研究地图与数据、证据判断与比较逻辑。

---

## V29 首页标题层级调整 + 装饰字迁移（2026-09-23）

完整记录见 `LANDING-TITLE-ROUND.md`。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **218/218** |
| `npm run e2e`（线上） | **66/66** |
| 两条真实路径实走（线上） | **62/62** |
| 线上指纹 | `1fe7282087` |
| 1440 主标题 | `ResearchPilot` 92px、深墨蓝、`h1Clipped=false`、`h1OverflowsContainer=false` |
| 390 主标题 | `ResearchPilot` 42.9px、无裁切；中文副标题 21px 紧接其下；两个入口底部 459px 仍在首屏 |
| 装饰字（更多页） | 1440 完整 `ResearchPilot`；390 只显示完整的 `Research`；不溢出、不压住标题 |
| 横向滚动条 | 1440 / 390 均无 |

### 未改动
产品文案、入口功能、研究地图与数据、证据判断与比较逻辑。
