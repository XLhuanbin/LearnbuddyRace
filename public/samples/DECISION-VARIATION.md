# 待完成验证项（本项目如实记录，不隐藏未完成内容）

本文件随预置语料一起发布，用于说明当前语料**已经过**与**尚未经过**的验证。

## 已经过真实模型验证

- 5 篇论文的 PDF 解析、字段抽取与实验条件抽取：真实模型调用（deepseek-chat，提示词 v2.1.0）。
- 每条引文均通过全文定位校验：34 条引文全部可再次定位，页码与重新定位结果一致。
- 程序校验层产出的问题清单（字段缺失 / 未提供引文 / 引文无法定位 / 页码异常 / 条件无法确认）。
- 不可比检测为**规则计算**，不依赖模型自评，可在本页「跨论文比较」中复算。
- 跨论文分歧分析：模型输出中的「结论分歧」会按规则复核，条件不一致时强制降级为「可由条件差异解释」。
- 用户条件化决策：见下方未完成项说明。

## 尚未完成

- **推荐随用户条件变化的验证**：需要两次真实模型调用（无算力条件 / 有算力条件）并对比推荐差异。
  本次尝试时模型接口返回 HTTP 402（余额不足），因此该验证**未完成**，脚本不会用预置结果替代。
  验证记录见仓库 `docs/DECISION-VARIATION.md`。

> 说明：界面上的「预置示例决策」是**另一次真实模型调用**的产物，已在界面明确标注为缓存结果；
> 它不能作为「推荐随条件变化」这一验收点的证据。
