# 首页标题层级调整 + 装饰字迁移（本轮）

日期：2026-09-23　线上：https://ce6edc09351c490baf62e9a4c1993ce2.app.workbuddy.host/
构建指纹：`1fe7282087`（与本地一致）

本轮只改**排版**：保留上一轮的暖纸色 / 深墨蓝 / 宋体风格，产品文案、入口功能、分析逻辑一律不动。

---

## 一、问题与改法

| 上一版的问题 | 本轮改法 |
| --- | --- |
| 最醒目的是中文句子，`ResearchPilot` 只是背景水印 | **把 `ResearchPilot` 升为首页真正的最大标题**：衬线 600、深墨蓝 `#17263c`、92px（1440）/ 42.9px（390），单行完整显示，不裁切、不折行 |
| `<h1>` 是中文长句，视觉焦点与语义焦点不一致 | `<h1>` 改为 `ResearchPilot`；中文主张降为紧随其下的 `<h2>`（32px / 21px，仍是衬线、仍醒目） |
| 两行文字、说明、按钮之间距离随意 | 重排节奏：眉标 → 品牌标题（下间距 14px）→ 中文主张（下间距 16px）→ 一句说明（下间距 30px）→ 两个入口 |
| 水印左右都被截断，排版不完整 | 首页**不再有水印**；装饰字迁移到「更多」页，桌面端完整显示 `ResearchPilot`，窄屏**有意只完整显示 `Research`** |

新增的类：`.landing h1.brand-title`（品牌主标题）、`.landing .cn-sub`（中文副标题）、`.morehead` / `.deco-wm`（更多页的装饰字）。

---

## 二、实测数据（线上）

| 检查 | 1440×900 | 390×844 |
| --- | --- | --- |
| 主标题文本 / 字号 / 颜色 | `ResearchPilot` / 92px / `rgb(23,38,60)` | `ResearchPilot` / 42.9px / `rgb(23,38,60)` |
| 主标题是否被裁切 / 溢出容器 | 否 / 否 | 否 / 否 |
| 主标题到中文副标题的间距 | 14px | 12px |
| 中文副标题字号 | 32px | 21px |
| 中文副标题到说明的间距 | 16px | 14px |
| 两个入口底部位置 / 是否在首屏 | 469px / 是 | 459px / 是（按钮整宽 362×51） |
| 横向滚动条 | 无 | 无 |
| 字体 | `RP Serif SC 600` 6 个子集全部 loaded | 同 |

「更多」页装饰字：

| 检查 | 1440 | 390 |
| --- | --- | --- |
| 实际显示文本 | `ResearchPilot`（完整） | `Research`（有意只显示完整的一半） |
| 字号 / 颜色 | 48.96px / `rgba(23,38,60,.12)` | 30px / 同 |
| 是否溢出右侧 / 是否压住标题 | 否 / 否 | 否 / 否 |

自动化：`typecheck` 0 error、`npm test` **218/218**、`e2e` **66/66**、两条真实路径实走 **62/62**（首页文案与首屏断言全部保持通过）。

---

## 三、截图

`docs/landing-title-round-live/`：
`1-首页-1440x900.png`、`2-首页-390x844.png`、`3-更多页-装饰字-1440x900.png`、`4-更多页-装饰字-390x844.png`

## 四、说明

- 装饰字全部在文档流内（不是绝对定位覆盖层），因此**不可能盖住功能内容**；窄屏通过隐藏 `Pilot` 段落实现「完整呈现 Research」，而不是靠裁切。
- 「更多」页的原有一句说明文案里仍提到「比较结果」和「方法演进」（那是更早版本的两个页面名，现已并入研究地图）。按本轮「不改文案」的要求我保留原样，需要的话下一轮可以一并改。
