# 开发状态（状态文件，随开发推进更新）

最后更新：2026-09-23（首页标题层级：ResearchPilot 成为最大标题，装饰字迁移到「更多」）
当前阶段：**正式领域已确定并落地为真实案例**（图像分类中 CNN 与视觉 Transformer 的方法演进与实验比较，5 篇真实论文，23 条实验记录）。

> 表述更正：先前「未完成项已全部补完」的说法不准确。**浏览器实时抽取**当时未验证（本轮已补做，30/30 通过）；
> 推荐正确性、证据绑定、关系候选检索当时未审计（本轮审计出 4 处缺陷并修复）。
> 仍未验证项见本文末尾与 `docs/ROUND5-AUDIT.md` §7。

| 项目 | 结论 |
| --- | --- |
| 主赛题 | 方向二 · 论文方法梳理智能体（已确认） |
| 细分领域 | **已确定**：图像分类（CNN 与视觉 Transformer）；旧 NLP 5 篇保留为独立回归样例 |
| 在线入口 | https://ce6edc09351c490baf62e9a4c1993ce2.app.workbuddy.host |
| 本地入口 | `cd researchpilot && npm run dev` → http://127.0.0.1:5173 |
| 截止时间 | 9 月 26 日 23:59（剩余约 10 天） |
| 模型接口 | **2026-09-17 15:12 起恢复可用**（此前 402 余额不足）；预置语料已用 v3 全量重跑 |
| 规则版本 | `RULES_VERSION = r3.0.0`（可比性 / 关系可信度 / 条件范围 / 分歧复核） |
| 提示词版本 | `PROMPT_VERSION = v3.0.0`（**预置语料已按 v3 全量重新生成**，与程序版本一致，不再标为过期） |

## 一、第二阶段新增能力（均已实现并通过程序校验 + 端到端走查）

### 亮点一：证据绑定

- 字段状态改为四态，且与界面文案一一对应：**可核验**（引文已定位）/ **待人工核对**（引文未定位）/ **未找到证据**（模型未给引文）/ **缺失**（论文未报告或未提取到）。
- 证据弹层展示：论文名称、页码（程序定位得到，不采信模型自报）、章节（启发式）、原文上下文（前后约 420 字符，高亮引文）。
- 未通过校验时**不显示页码**，改为展示「实际匹配到的原文位置」，说明模型引文的哪一部分是真的。
- 页码程序校验：模型自称页码与定位结果不一致时标为「页码与定位结果不一致」；超出论文页数时标为「页码超出论文数」，界面一律以程序定位结果为准。

### 亮点二：不可比检测

- 新增结构化实验条件：数据集、数据划分、评价指标、额外训练数据、预训练模型、实验设置、算力/训练时长（7 个维度，各自带引文与页码）。
- 可比性判断**由规则计算**（`src/core/comparability.ts`），不交给模型自评，因此每个结论都能说明依据：
  - 数据集 / 指标：完全一致 → 可直接比较；部分重叠 → 只能有限比较；无共同项 → 不能直接比较；
  - 额外训练数据：不一致即**不能直接比较**；
  - 实验设置：先识别评估协议（微调 / 零样本 / 少样本 / 蒸馏 / 从头训练），协议不重叠 → 不能直接比较；协议一致但超参不同 → 可直接比较并说明差异；协议无法识别 → 信息不足；
  - 数据划分 / 预训练模型：不一致 → 只能有限比较；
  - 算力/训练时长：只用于说明「能否确认可运行性」，不参与性能可比性判断。
- 每个维度都列出：结论、具体差异、判断依据（区分「原文证据 / 信息缺失 / 判定规则」）、缺失信息。
- 条件不一致时明确禁止直接比较数值；即使条件一致，系统也不自动排名。

### 亮点三：方法关系可信度

- 关系可信度改为三档，**不使用未校准的置信度百分比**：**原文明示** / **系统推断** / **待核查**。
- 程序强制规则（`src/core/validate.ts`）：
  - 原文明示必须有定位成功的引文，否则降级为「待核查」；
  - 引文即使可定位，若既不含被继承方法名、也无引用标记、也无继承/改进措辞（如 based on / builds upon / improvement over），判为「关键词相似而非技术继承」并降级；
  - 系统推断必须有实质推断理由（过短或仅凭共同关键词归类则降级）；
  - 关系两端方法名缺失则降级为「待核查」。
- 关系详情展示：来源论文、支持片段（可打开完整上下文）、AI 判断理由、可信度含义、以及**程序调整可信度的记录与原因**。
- 人工修正：可改关系类型、可信度与理由，可删除、可人工新增关系；保留 **AI 原判定**并可一键恢复；人工改动会被标记。

### 亮点四：根据用户条件进行方法决策

- 输入：基础、研究目标/兴趣、可用时间、计算资源、具体目标。
- 输出：**候选方法**（匹配度 条件匹配 / 有条件可用 / 无法判断）、**阅读顺序**、每条推荐的理由（标注依据来源：你的条件 / 论文 / 信息缺口）、适用条件、缺失信息、以及「条件敏感性」说明。
- 论文未报告算力时置 `computeReported=false`，明确说明无法确认可运行性，不推测。
- 下载的算力信息（如 8×P100 3.5 天、1024×V100、8×16GB V100 90h）均带原文证据页码。

### 亮点五：跨论文分歧与待调查问题

- 发现类型四档：**结论分歧（条件一致）** / **可由条件差异解释** / **共同提到的局限** / **当前材料未发现**。
- 程序强制复核：模型若把条件不一致的论文对判为「结论分歧」，系统会**强制降级**为「可由条件差异解释」并列出具体条件差异。
- 每条发现展示：涉及论文、双方结论与原文片段、条件差异、可能解释、下一步核查动作、以及固定声明。
- 同时输出「已检查的论文对」清单（含每对的可比性结论），用于证明没有为了展示而制造冲突。
- 共同局限的声明明确写为「当前材料中存在待调查问题，不代表整个领域存在研究空白」。

### 程序校验层（新增，独立于模型）

`src/core/validate.ts` 产出问题清单（字段缺失 / 未提供引文 / 引文无法定位 / 引文过短 / 页码不一致 / 页码无效 / 条件无法确认 / 关系被降级 / 关系缺少依据），
在论文库中逐条展示，并在状态页汇总统计。所有问题都说明「系统已做了什么处理」或「需要人工做什么」。

## 二、实际运行的验证结果（真实模型，deepseek-chat，提示词 v2.1.0）

| 指标 | 实际结果 |
| --- | --- |
| 样例论文 | 5 篇（Attention Is All You Need / BERT / RoBERTa / DistilBERT / GPT-3），年份 2017/2018/2019/2019/2020 全部识别正确 |
| PDF 解析 | 全部成功，13~75 页，18,778~240,327 字符 |
| 字段抽取 | 35 个字段中 **34 个有值，34 个引文全部通过全文定位校验**，0 个未找到证据，1 个缺失 |
| 实验条件 | 每篇可核验 4~7 个维度（共 7 个）；所有条件条目均带引文与页码 |
| 程序校验 | 共发现 8 个问题：条件无法确认 4、页码与定位不一致 2、字段缺失 1、引文无法定位 1 |
| 方法关系 | 7 条：原文明示 1、系统推断 5、待核查 1；其中 **1 条被程序从「原文明示」降级为「待核查」**（引文可定位但不含继承措辞） |
| 不可比检测 | 全部 5 篇一起比较 → **当前不能直接比较**；只选 BERT/RoBERTa/DistilBERT → **只能有限比较**（端到端走查中实测两种结论都出现） |
| 分歧发现 | 2 条「共同提到的局限」+ 1 条「当前材料未发现」；**没有产生任何「结论分歧」**（因条件不一致，程序不允许） |
| 已检查论文对 | 10 对，全部列出检查结果 |

### 自动化验证

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test`（178 项，未改动） | 全部通过：证据定位与负样本、归一化边界、不可比检测（含范围与未知判定）、论文对级一致性、关系证据充分性、分歧规则复核、页码与页数 |
| `npm run e2e`（112 项，本地 + 线上各一次） | 全部通过：7 个入口、四态字段、实验条件表（含适用范围）、证据弹层、两种可比性结论、关系证据充分性判定与人工修正、决策示例、分歧页（含程序复核）、版本与过期面板、0 控制台错误 |
| `npm run build` | 成功，产物 app.js 623KB / pdf.worker 1.38MB |

### 自动化检查发现并修复的真实问题

0. **第三轮（正确性）**：比较页与分歧页结论不一致（论文对级复用全集结论）、额外训练数据用局部实验描述支撑整篇结论且「取值相同即判一致」、关系降级理由与原文矛盾（方法名整串匹配失败 + 把方法专名当泛化词）、推荐理由挂错证据并混用预训练/微调成本、条件差异表述不准确、页数显示 0。详见 `docs/CORRECTNESS-FIXES.md`。
1. **连续快速点击选择论文时状态互相覆盖**（比较页用陈旧的选择集计算）→ 改为函数式状态更新。
2. **端到端断言命中页面静态说明文案造成假阳性**（把「只能有限比较」写在页面描述里，断言误判为通过）→ 断言改为只在该面板 DOM 内取文本，随后才真正区分出两种可比性结论。
3. **部署后访问者仍看到旧版本**：固定资源文件名被 CDN 按 URL 缓存（实测 Age 3160 秒、返回旧文件）→ 新增构建指纹脚本，把 `index.html` 中的资源引用改写为 `app.js?v=<内容哈希>`；实测带查询串的请求返回最新文件，线上端到端 58 项全部通过。

## 三、阻塞与限制

| 事项 | 状态 |
| --- | --- |
| ~~模型接口余额不足~~ | **已解除**：2026-09-17 15:12 起接口恢复，已用 v3 全量重跑 5 篇预置语料（字段 35/35 有值、32 条引文可核验、算力 5/5 带阶段）。 |
| ~~「推荐随用户条件变化」未验证~~ | **已完成**：两组条件（笔记本两周 / 8 卡 A100 半年）各跑一次真实决策，阅读顺序与候选匹配度均发生有依据的变化（RoBERTa 由 conditional 变为 suitable）。记录见 `docs/DECISION-VARIATION.md`。 |
| ~~未预置论文真实导入~~ | **已完成**：用 DDPM 论文（2006.11239，25 页，非预置）验证，7/7 字段可核验；并借此发现并修复「标题抓到摘要句」问题（新增 `looksLikeTitle` 与 `title_unverified`）。记录见 `docs/IMPORT-VERIFICATION.md`。 |
| 实时抽取链路的端到端走查 | 未执行（无头走查是在未配置密钥下跑的，覆盖缓存路径与降级行为；无接口时系统不会伪造结果，该行为已被验证） |
| ~~导出文件内容抽查~~ | **已完成**：新增 8 项导出用例（可比性小节、四态口径、原文引文、页码、不做排名声明、无密钥形态）+ 产物密钥扫描（dist 34 个文件无密钥）。 |
| 正式领域语料（8~12 篇） | 未开始，等待领域确认 |
| OCR / 扫描件、自动全网检索、失败经验库、多用户协作 | 明确不做（首版范围外） |
| 服务端密钥保管 | 不支持（部署通道为纯前端静态托管） |

## 四、运行方式速查

```bash
cd researchpilot
npm install --registry=https://registry.npmmirror.com
npm run dev                 # 本地开发 http://127.0.0.1:5173
npm run typecheck           # 类型检查
npm test                    # 核心逻辑回归（149 项）
npm run build               # 构建 dist/（含资源指纹注入）
npm run e2e                 # 端到端走查（先 build；可加 --url 指定线上地址）
npm run samples             # 下载样例论文 PDF
npm run analyze             # 真实模型完整流水线（抽取 + 关系 + 分歧 + 示例决策）
npm run analyze:report      # 只用已有缓存重建报告，不调用模型
npm run variation           # 验证「推荐随用户条件变化」（需模型可用）
```

## 五、下一步（按优先级）

1. **恢复模型额度**后立即补做两项验收：`npm run variation`（推荐随条件变化）+ 用一篇未预置的新论文做实时导入验证。
2. 确认细分领域 → 准备 8~12 篇正式语料并登记来源与许可 → 重跑流水线 → 人工逐篇核对。
3. 导出文件内容抽查、重复上传去重、刷新恢复、模型超时与失败重试的用例补充。
4. 前端细节打磨（关系图缩放/筛选、移动端）。
5. 3 分钟视频、PPT、依赖许可清单与 LearnBuddy 使用记录。

## 六、2026-09-17 补完（额度恢复后的执行结果）

详见 [`ROUND4-COMPLETION.md`](ROUND4-COMPLETION.md)。要点：

1. **v3 全量重跑**：字段 35/35 有值、32 条引文可核验、实验条件 9 个维度且带适用范围与算力阶段、关系 4 条全部为系统推断并逐条说明依据不足的原因、分歧 1 条「各自局限」。
2. **修复决策证据挂载缺陷**：模型把条件维度标成 `field` 时程序查不到证据，导致 5 条资源理由被误标「无证据」；修复后有证据理由 15 条、资源类理由全部带阶段标签。
3. **条件对照验证**：两组用户条件下的阅读顺序与候选匹配度均发生变化且有依据（RoBERTa 因算力条件改善由 conditional → suitable）。
4. **未预置论文验证**：DDPM 论文 7/7 字段可核验；发现并修复「启发式标题抓到摘要句、模型回显同一句」的问题。
5. **新增走查**：刷新恢复、窄屏 390×844（新增 ≤860px 布局适配）、构建产物密钥扫描。
6. **测试规模**：`npm test` 149/149；`npm run e2e` 84/84（本地与线上各一次）。

## 七、2026-09-17 第五轮审计（推荐正确性 / 证据引用 / 真实浏览器流程）

完整记录见 [`ROUND5-AUDIT.md`](ROUND5-AUDIT.md)。要点：

1. **RoBERTa 可行性判断过乐观**：模型给出 `suitable`，但论文只给了预训练算力（1024×V100）、没有显存/并行/配置证据。
   新增程序判定 `resourceFeasibility`，并把 `suitable` 收紧为「阶段一致 + 规模可比 + 有显存/并行/配置证据」三条件同时成立；
   修正后四个条件下**没有任何候选被认证为 suitable**，全部为 conditional/unknown。
2. **单变量条件对照重做**：只改算力、只改时间各自一组，另加「笔记本无 GPU · 两年」反例。
   反例中 suitable 且目标为训练阶段的候选 = 0；「只改时间」不会改变 RoBERTa 的可行性状态（不用时间折算 GPU）。
3. **evidenceRef 绑定收紧为可审计流程**：唯一定位/类型规范化/无效引用拒绝/跨论文拒绝/阶段一致性，且「定位成功」与「支持理由」分开记录。
4. **关系候选检索改进**：新增句子级候选检索并进入提示，模型得以挑中真实存在的句子；
   改进后 3 条关系被认证为原文明示（引文均为真实论文句子）。同时收紧标准（第三方主语守卫、必须指向关系另一端）。
   另修复一处别名缺陷：`RoBERTa (Robustly Optimized BERT Pretraining Approach)` 的括号内含 "BERT"，旧实现把它当成 RoBERTa 的别名。
5. **真实浏览器流程已验证**：上传非预置论文 → 浏览器发起真实模型调用 → 结构化结果 → 证据弹层 → 保存 → 刷新恢复，
   30/30 通过；鉴权失败路径用本地 mock 确定性验证 7/7 通过。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **114/114** |
| `npm run e2e`（本地 + 线上） | **74/74** |
| `browser-flow-check`（线上，真实模型） | **30/30** |
| `auth-error-check`（本地 mock） | **7/7** |
| 线上构建指纹 | `7b35461dbe` |

**仍未验证**：真实接口的鉴权失败提示（观察窗口内服务未返回响应）；
模型判定在多次重复下的稳定性（条件对照每组仅跑一次）；关系候选句的语义仍建议人工复核；论文级初筛限制不变。

## 八、2026-09-18 第六轮（基础体验收尾 + 领域准备）

完整记录见 [`ROUND6-COMPLETION.md`](ROUND6-COMPLETION.md)。要点：

1. **目标匹配与执行可行性拆开**：`goalMatch`（为什么值得研究，不受硬件影响）+ `execution[]`（阅读/推理/微调/预训练四种操作各自判定）。
   阅读不因预训练门槛被阻断；微调只用微调阶段证据（明确写「不能用预训练成本代替」）；界面标明「论文证据判断 ≠ 实际执行验证」。
2. **恢复重新分析入口**：已分析论文可直接「重新分析」，两步确认（提示消耗额度）；成功后更新并提示下游需重新生成
   （关系/决策需重跑，比较按当前数据实时重算）；失败或取消保留原结果；人工修正合并保留。
3. **超时/取消确认**：超时归类为「失败（未完成）」、不停留在运行中；取消不重试且文案明确「不代表服务端已停止计算或不再计费」；
   本地 mock 9 项确定性验证通过；真实接口的异常表现保持「未验证」。
4. **三个正式领域候选**（未选定）：三维点云语义分割 / 低资源视觉表征与轻量模型 / RAG 长文档问答；
   书目用 arXiv API 逐条核实（18 篇，见 `docs/DOMAIN-REFERENCES.md`），方法关系线索预检见 `docs/DOMAIN-RELATION-PROBE.md`。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **143/143** |
| `npm run e2e`（本地 + 线上） | **84/84** |
| 真实浏览器流程（线上，真实模型） | **35/35** |
| 线上构建指纹 | `8f69f52074` |

**必要缺项**：正式细分领域未确定（当前仍为开发验证样例）；推理阶段缺「权重/推理成本」维度故恒为未验证；
真实接口异常路径未验证；五篇样例无显存/并行/配置级算力证据（因此无 suitable，属如实结论）。


## 九、2026-09-18 补充（收掉两处可行动缺项）

1. **推理行不再一律「未验证」**：新增确定性全文检索，用论文原文证明「权重/代码可获取」；
   BERT / RoBERTa / DistilBERT / Transformer 有原文依据，GPT-3 如实标为未验证（确实未发布权重）。
   文案明确「可获取 ≠ 可运行」。
2. **真实浏览器验证重新分析成功路径**：人工修正 → 重新分析 → 成功后修正仍在、下游待更新横幅出现；
   顺带修正「资源类理由引用非算力证据」的不一致（现在不挂载，只记录原因）。
3. 验证：`npm test` 149/149；`npm run e2e` 84/84（本地+线上）；浏览器流程 45/45；线上指纹 `fede50534a`。
## 九、2026-09-18 正式视觉案例（本轮）

完整记录见 [`VISION-CASE-REPORT.md`](VISION-CASE-REPORT.md)，
逐条核查清单（原文 / 系统输出 / 判断）见 [`VISION-REVIEW-CHECKLIST.md`](VISION-REVIEW-CHECKLIST.md)，
表格行列核查见 [`EXPERIMENT-TABLE-CHECK.md`](EXPERIMENT-TABLE-CHECK.md)。

1. **语料**：ResNet / ViT / DeiT / Swin / ConvNeXt（arXiv 1512.03385 / 2010.11929 / 2012.12877 / 2103.14030 / 2201.03545），
   文件核对见 [`VISION-CORPUS-FILES.md`](VISION-CORPUS-FILES.md)；文件名年份只作预印本年份，会议信息在文件中无依据的一律标待核实。
2. **实验级抽取**：一篇论文可有多条实验记录（模型变体、预训练/训练数据、评估集与划分、分辨率、指标与数值、
   额外数据、蒸馏与教师、测试时增强、单模型/集成、参数量/FLOPs/吞吐量），共 23 条（分类 20、检测 3）。
3. **表格核查**：文本层（引文 + 表题 + 行标签）全部通过；**版面几何核查**（数值所在行是否含行标签、该列向上的列头是否与指标一致）
   2 条确认、21 条标为待核查（双栏/跨行排版无法确认列对齐），待核查记录不进入「可直接比较」。
4. **四个亮点**：可比性（预训练数据 / 分辨率 / 蒸馏 / error-vs-accuracy 四类条件差异）、方法关系（10 条，三档证据状态）、
   阅读与执行建议（目标匹配 + 四阶段可行性）、分歧（2 条，含 condition_confounded）。
5. **界面**：新增「实验比较」页；「论文库」可切换语料集（正式案例 ↔ 开发回归样例），两套结果各自独立、不混合生成关系与推荐。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **178/178** |
| `npm run e2e`（本地 + 线上） | **99/99** |
| 真实浏览器流程（上一轮能力仍有效） | 35/35 |
| 线上构建指纹 | `73fe73cbc3` |

**已知不足**：ViT 有一条记录的指标名抽成了数据集名（未修复，按原样展示并在清单标出）；
Swin 三条 1K-only 记录的预训练字段缺失；浏览器端实时抽取尚未覆盖实验记录字段。

## 十、2026-09-18 前端与引导改版

完整记录见 [`UI-UX-ROUND.md`](UI-UX-ROUND.md)；逐页截图见 `docs/ui-audit/` 与 `docs/demo-walkthrough/`。

1. **新增「开始」首屏**：一句话产品说明 + 三个问题 + 6 步演示路径（带完成状态）+ 当前语料集卡片 + 折叠的规则说明。
2. **导航改为任务顺序**：开始 / 论文库 / 实验比较 / 方法关系 / 阅读决策 / 分歧与待调查 / 跨论文比较 / 状态与记录 / 设置，
   每项带状态点；编号保留为次要信息；刷新会回到上次所在页。
3. **论文库**：语料集卡片（名称、5 篇论文、正式/回归标记、主入口）、引导式空状态、模型未配置提示压缩为一行、加载后给「下一步」。
4. **实验比较页重构**：紧凑表格 + 筛选（按论文/搜索/只看已确认）+ 固定选中摘要 + 对照卡片上移 + 推荐对照常驻 +
   修复「正面说明被显示为警告色」+ 可见的「不做排名/不给因果结论」声明。
5. **可访问性**：Tab 焦点环（实测 outline solid 2px）、状态不依赖颜色、弹层可关闭、窄屏表格容器内滚动且整页无横向溢出。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | 178/178（未改动核心逻辑） |
| `npm run e2e`（本地 + 线上） | **112/112** |
| 新用户路径实走（本地 + 线上） | **35/35** |
| 线上构建指纹 | `0a7be35d2f` |

**约束遵守**：本轮未修改任何实验数值、判定规则、证据引文与推荐结论；待核查记录仍标「待核查」。

## 十一、2026-09-20 浅色视觉重构与交互重组

完整记录见 [`VISUAL-REDESIGN-ROUND.md`](VISUAL-REDESIGN-ROUND.md)；
过程截图见 `docs/ui-audit-light2/`；路径实走截图见 `docs/demo-walkthrough-v2-live/`。

1. **视觉方向**：从深色控制台改为「面向科研初学者的 AI 文献研究工作台」——白 / 暖灰底、深墨文字、靛青主色，
   琥珀＝警告、橙＝待核查、绿＝已核验；统一圆角与阴影，去掉装饰性渐变与大面积深色块。
2. **外壳重组**：顶栏（品牌 + 定位 + 设置 / 开发状态）+ 左侧任务流导航（开始 / 论文库 / 实验比较 / 方法关系 / 阅读建议 / 待调查问题，
   跨论文比较降级到「更多」）+ 面包屑 + 轻量进度（不锁定步骤，可随时返回）。
3. **首屏重做**：主文案「别急着比较分数，先确认论文到底在比较什么。」+ 主 CTA「开始分析视觉论文」+ 次 CTA「查看演示案例」+ 三步流程；
   状态数字、版本号、规则说明与免责声明全部移出首屏。
4. **论文库**：案例信息条（5 篇 / ImageNet 图像分类 / 23 条实验记录）+ 主按钮「加载演示案例」+ 论文卡片方法标签与证据状态，
   动作收敛为「查看论文 / 查看实验」。
5. **实验比较（核心工作区）**：左筛选与选择、右大结论卡（档位 + 人话解释 + 具体原因 + 大号数值 + 原文证据主按钮），
   条件差异与完整条件表折叠在「查看详细条件差异」；默认给出一组推荐对照。
6. **方法关系**：先「方法演进摘要」（取自各篇核心思路字段并标注证据状态），后关系图。
7. **状态语言统一为 7 种**（实时分析 / 缓存案例 / 人工修正 / 已核验 / 待核查 / 信息不足 / 已过期），图标 + 文字 + 颜色三重表达；
   文本可定位但版面行列未确认一律「待核查」，不使用成功绿。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | 178/178（核心逻辑未改动） |
| `npm run e2e`（本地 + 线上） | **112/112** |
| 路径 A/B 实走（本地 + 线上） | **41/41** |
| 线上构建指纹 | `ae43112683` |

**数据结论未改动**：实验数值、证据引文、可比性判定、关系状态与推荐内容全部原样；待核查仍为待核查。
**本轮发现（非界面问题）**：预置语料中跨论文分类实验对的可比性等级为「不能直接比较 112 对、信息不足 48 对、
可以直接比较 0 对、只能结合条件讨论 0 对」——原因是 ResNet 报错误率而其余报准确率，且部分记录缺蒸馏/测试时增强等字段。
界面保留真实结论，并补充「口径不同的例子」入口演示不能直接比较；补齐字段属于下一轮的抽取侧工作。

## 十二、2026-09-20 语料状态与加载流程修复

完整记录见 [`CORPUS-STATE-FIX.md`](CORPUS-STATE-FIX.md)。

**根因（实测确认）**：
1. 早期论文/方法没有 `corpusId`，且左侧计数用全量 `papers.length`，与论文库的按语料过滤不一致 →
   旧数据留在状态里不可见却被计数，再次加载时按 ID 过滤而不是按语料范围替换 → 5 篇变 10 篇；
2. 论文库的 `onClick={onLoadSample}` 把 React 事件当成了 `loadSample(key)` 的参数 → 加载静默失败。

**修复**：
- 新增 `src/core/corpus.ts`：语料集稳定身份（vision-classification / nlp-dev / user-import）、论文 ID 派生、
  历史数据归位、`scopeCorpus()` 范围计算、`scopeRelations()` 端点过滤；
- 导航 / 首页 / 论文库 / 实验比较 / 方法关系的计数**全部**改用当前语料范围；
- `loadSample` 改为**按语料集幂等替换**（先删该语料已有记录再写入），加载互斥 + 完成/失败反馈 + 失败保留数据；
- 语料集切换改为**替换而非叠加**（移除上一套预置数据，自传论文不受影响）；
- 论文库新增「检测到其它语料集的数据」提示 +「切换过去 / 清理这些数据」两个明确动作；
- 实验比较页空状态区分「没有语料」与「有论文但无实验记录」，并调整信息优先级（语料状态 → 问题 → 推荐对照 → 结论 → 详情）。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **193/193**（新增语料身份/范围/归位 15 项） |
| `npm run e2e`（本地 + 线上） | **112/112** |
| 语料状态验收（A/B/C + 切换往返，本地 + 线上） | **22/22** |
| 路径 A/B/D/E 实走（本地 + 线上） | **41/41** |
| 线上构建指纹 | `e9ce620cbc` |

**数量对比**：空会话加载 5 篇 / 23 条 / 10 条关系（IndexedDB 同为 5/5/10）；重复点击与刷新后仍不变；
历史 NLP 数据不计入正式案例（标为 nlp-dev 并可清理）；切换往返后 IndexedDB 只剩当前语料。

## 十三、2026-09-21 信息架构重构（宣传首页 + 结果工作台）

完整记录见 [`INFO-ARCH-ROUND.md`](INFO-ARCH-ROUND.md)；
截图见 `docs/demo-walkthrough-v4-live/` 与 `docs/ui-audit-demo/`。

1. **宣传型首页**：只有一句话定位（「别急着比较论文分数，先确认它们到底在比较什么。」）、一句话副标题、三个关键词、
   一个抽象示意图（两个结果 → 一个结论）、一个主按钮「进入 Demo」与次按钮「查看示例结果」；**不渲染侧栏**，
   不显示数量、版本、规则、配置、缓存与开发状态。
2. **导航极简**：首页 / 比较结果 / 方法演进 + 更多。论文库、全部结果、方法关系图、阅读建议、待调查问题、
   跨论文比较、开发状态、设置、开发回归样例（NLP）全部收进「更多」。
3. **案例选择页**：单一正式案例「ImageNet 图像分类方法演进」+ 主按钮「开始查看案例」。
4. **结果页**：两个结果卡片（方法 · 变体 / 数据集 / 大号数值 / 指标）→ 大号结论 + 一句人话原因 →
   「一致 / 不同 / 仍需确认」三列可视化 → 三个折叠区（为什么这样判断 / 哪些条件不同 / 查看原文依据）。
5. **方法演进**：一条路线（方法名 + 一句话贡献 + 关系标签），点击节点看「做了什么 / 与前一个方法的关系 / 原文依据」；
   底部给「建议按这个顺序阅读」1–5 条。
6. **术语抽象**：实验记录→论文结果，语料集→论文集合，证据绑定→原文依据，表格行列待核查→仍需确认。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **193/193**（未改核心逻辑） |
| `npm run e2e`（本地 + 线上） | **117/117** |
| 语料状态验收（本地 + 线上） | **22/22** |
| 新用户路径实走（本地 + 线上） | **37/37** |
| 线上构建指纹 | `a5b2b051b7` |

**数据结论未改动**：模型输出、实验数据、证据引文、实验记录、比较规则与推荐算法原样；待核查仍标「仍需确认 / 待核查」。

## 十四、2026-09-21 定位纠偏与研究地图（三个视图）

完整记录见 [`POSITIONING-MAP-ROUND.md`](POSITIONING-MAP-ROUND.md)；截图见 `docs/demo-walkthrough-v5-live/`。

**纠偏**：主赛题回到「论文方法梳理智能体」——看懂方法、理清关系、决定先读什么；
「两个分数能不能直接比较」降为方法对照中的一项能力（在「联系与区别」里作为可展开的下一步）。

1. **首页**：主标题「把一组论文，变成你看得懂的研究地图。」+ 副标题（看懂方法 / 彼此如何发展 / 先读哪篇 + 原文依据）；
   关键词：看懂方法差异 · 理清技术关系 · 决定先读什么；示意图「论文 → 分组与关联 → 阅读路线」（标注为示意）；
   主按钮「体验视觉论文案例」（加载案例并直接进入研究地图），次按钮「上传我的论文」。
2. **导航只有三项**：首页 / 研究地图 / 更多。
3. **研究地图三视图**（共用同一组论文与选中状态）：
   - 看懂方法：领域概览（系统汇总）+ 方法分组 + 方法卡片（解决什么问题 / 核心做法 / 最明显的区别），局限与依据点击后展开；
   - 联系与区别：只含真实关系的关系表（含证据状态与原文依据）+ 独立节点说明 + 发表顺序时间线（标注不代表继承）+
     选两篇对照（解决什么问题 / 核心思路 / 局限 / 数据集 / 指标）+ 可展开的「比较实验表现」；
   - 从哪里开始：先问基础与目标，选到复现/微调/训练才问时间与算力；输出阅读顺序、每篇重点与理由。
4. **新增 `src/core/grouping.ts`**：方法归组与领域概览由程序从已抽取字段派生（关键词规则 + 命中不了的进「暂未归类」），
   不按论文 ID 硬编码；新增 10 项单元测试。
5. **上传自己的论文**：`UploadFlowView`——解析 → 需要模型时就地配置 → 处理进度 → 进入同一工作区；单篇明确提示补足论文，不编造跨论文关系。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **205/205** |
| `npm run e2e`（本地 + 线上） | **121/121** |
| 语料状态验收（本地 + 线上） | **22/22** |
| 两条真实路径实走（本地 + 线上） | **52/52** |
| 线上构建指纹 | `04b302feb1` |

**未完成项**：模型抽取的成功路径未实测（当前无可用密钥）；方法归组规则对更细领域需扩充；
概览措辞需真实用户读一遍再决定是否润色。

## 十五、2026-09-21 研究地图（分析层修正 + 可交互地图）

完整记录见 `METHOD-MAP-ROUND.md`；截图见 `docs/demo-walkthrough-v6-live/`。

**分析层**（`src/core/grouping.ts` 重写）：
1. 方法家族（单一）只看「方法名称 + 核心思路首句」，名称优先；新增 `stripReferenceMentions()` 剔除
   「以 X 为起点 / 借用 X 的技巧 / 引入 X 的设计 / X 教师」等引用语境，因此「提到 CNN 教师」不会被判成 CNN 架构；
   判不出进「待确认」，每条判定记录依据来源与命中关键词。
2. 概览统计：数据集/指标先单篇内去重再跨论文计数；文案区分「全部 / 部分（标篇数）/ 未知」。
3. 关系方向：`relationSentence()` 从任一端查看都按 from → to 生成同一方向说明；相似/不明确关系不声明方向。

**界面层**：新增 `src/ui/MethodMap.tsx`（泳道 = 方法家族；节点 = 短名称 + 一句话贡献 + 策略标签；连线只画真实关系，
颜色 + 线型双重区分；「关系不明确」默认不连线、待核查默认不显示；点节点高亮 + 侧边详情 + 原文依据折叠；点连线给方向、
证据状态与具体联系；缩放/重置；窄屏不缩小图形）。`MapView` 默认进入「方法地图」。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **213/213**（新增/重写 12 项回归） |
| `npm run e2e`（本地 + 线上） | **127/127** |
| 两条真实路径实走（本地 + 线上） | **62/62** |
| 语料状态验收（本地 + 线上） | **22/22** |
| 线上构建指纹 | `033d4e0c9c` |

**未改动**：论文数据、证据内容、实验记录、关系数据与判断算法、人工修正与迁移逻辑。
**剩余问题**：窄屏仍为横向泳道；正式案例「原文明示」为 0 条；家族规则为关键词规则需按领域扩充；模型抽取成功路径仍未实测。

## 十六、2026-09-23 研究地图：探索画布重设计 + 集合内容修正

完整记录见 `CANVAS-REDESIGN-ROUND.md`；截图见 `docs/demo-walkthrough-v7-live/`。

**内容修正（线上出现 10 篇 / BERT 混入）**：根因是 `MapView` 把「当前语料预置论文」与「用户自传论文」拼接渲染，
且家族名写死为「视觉 Transformer」。修法：
1. 集合范围分离——地图只有「案例」（当前语料预置）与「我上传的论文」两种集合，永不静默混合，工具栏显示当前集合；
2. 案例为空但用户已有论文时，自动显示「我上传的论文」；
3. 家族名中性化：`Transformer 架构` / `卷积网络（CNN）` / `混合（卷积 + 注意力）` / `待确认`；
4. 用户论文与历史记录一条未删；
5. 新增 `scripts/verify-collection-scope.mjs`（14 项）回归。

**画布重设计**：一条紧凑工具栏（集合名 + 三个视图 + 添加论文 + 选项）+ 一整块画布 + 点击后的浮层详情；
待核查 / 关系不明确开关、重新加载、更换案例、领域概览与统计、模型状态与本机存储说明全部收进「选项」；
画布内只保留一句轻提示；连线标签默认不铺（悬停 / 聚焦 / 选中才出现）；节点只显示短名称、弱化年份与一条短贡献
（短贡献为原文连续子串，单元测试有断言）；泳道改为轻量分区；详情面板桌面右侧、窄屏底部，关掉不丢视角。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **218/218** |
| `npm run e2e`（本地 + 线上） | **66/66** |
| 新用户两条路径实走（本地 + 线上） | **62/62** |
| 集合范围回归（本地 + 线上） | **14/14** |
| 语料状态回归（本地 + 线上） | **22/22** |
| 线上构建指纹 | `6ce7b325c8` |

**已知问题**：e2e 覆盖从 127 项降到 66 项（脚本被误截断后重建，需补回专业视图的细粒度断言）；
窄屏仍为横向滑动泳道；正式案例「原文明示」为 0 条；家族判定仍是关键词规则；模型抽取成功路径未实测。

## 十七、2026-09-23 首页重设计 + 全站配色统一

完整记录见 `LANDING-REDESIGN-ROUND.md`；截图见 `docs/landing-redesign-live/`。

1. **色彩体系替换**（`src/styles.css` 令牌）：暖纸底 `#fcf8f1` / `#f5efe3`、深墨蓝文字与主色 `#17263c` / `#1e3a5f`、
   少量铜色 `#9e5326` 用于细线与编号；语义色重设为 深绿=已核验 `#2f6b4a`、暖橙=待核查 `#be6a1e`、
   灰蓝=信息不足 `#4e5a6b`、琥珀=警告 `#8a5a12`、砖红=不可比 `#8e322c`。
   状态仍是「图标 + 文字 + 颜色」三重表达，不靠颜色单独承载语义。顶栏、按钮、功能页、研究地图画布一致生效。
2. **首页重排**：超大 `ResearchPilot` 水印（252px、墨蓝 5%、纯装饰层、aria-hidden）；
   铜色眉标 + **衬线主标题**（单行、47.5px，视觉中心）+ 单行副标题 + 两个入口（首屏内可点）；
   三项能力改为编号 + 细分隔线的排版块（不再堆卡片）；流程示意改为一条轴线 + 三个节点 + 三段说明，并标明「非真实分析结果」。
3. **字体**：自托管 **Noto Serif SC**（思源宋体 Google 版，SIL OFL 1.1）子集，6 个 woff2（约 231 KB）放 `public/fonts/`，
   `src/fonts.css` 声明 `@font-face`（含 `unicode-range`、`font-display: swap`）；不依赖 Windows 宋体，未命中子集自动回退。
4. **修复构建指纹**：`scripts/stamp-build.mjs` 原先只哈希 `app.js`，仅改样式时指纹不变 → 线上继续命中旧 CSS。
   现把 `index.css` 一并哈希。

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 0 error |
| `npm test` | **218/218** |
| `npm run e2e`（线上） | **66/66** |
| 两条真实路径实走（线上） | **62/62** |
| 集合范围回归（线上） | **14/14** |
| 线上构建指纹 | `fd0ad5ae64` |
| 字体 / 溢出 | 6 个子集 loaded、无网络失败；1440 与 390 均无横向溢出 |

**未改动**：产品定位、两个入口的功能、研究地图与数据、证据判断与比较逻辑。

## 十八、2026-09-23 首页标题层级调整 + 装饰字迁移

完整记录见 `LANDING-TITLE-ROUND.md`；截图见 `docs/landing-title-round-live/`。只改排版，文案与功能不动。

1. **`ResearchPilot` 升为首页最大标题**：`<h1 class="brand-title">`，衬线 600、深墨蓝 `#17263c`、
   `clamp(34px, 9.6vw, 92px)`、`white-space: nowrap`（任何宽度都完整成行，不裁切、不折行）。
2. **中文主张降为副标题**：`<h2 class="cn-sub">`，32px（1440）/ 21px（390），紧随主标题之下（间距 14px / 12px）。
3. **重排首屏节奏**：眉标 → 品牌标题 → 中文副标题 → 一句说明 → 两个入口（间距 14 / 16 / 30px）。
4. **首页不再有背景水印**；装饰字迁移到「更多」页（`.morehead` + `.deco-wm`）：
   桌面完整显示 `ResearchPilot`，窄屏**有意只显示完整的 `Research`**；装饰字在文档流内，不会盖住内容。

| 检查 | 1440×900 | 390×844 |
| --- | --- | --- |
| 主标题 | ResearchPilot / 92px / 深墨蓝，无裁切 | ResearchPilot / 42.9px，无裁切 |
| 中文副标题 | 32px，紧接其下 14px | 21px，紧接其下 12px |
| 两个入口 | 底部 469px，首屏内 | 底部 459px，首屏内 |
| 横向滚动条 | 无 | 无 |

`typecheck` 0 error；`npm test` 218/218；`e2e` 66/66；两条真实路径 62/62；线上指纹 `1fe7282087`。
