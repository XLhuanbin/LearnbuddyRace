/**
 * 已废弃：本组件原先以「论文卡片列表」形式展示方法。
 *
 * 本轮起，研究地图的首屏主体是图形（见 `src/ui/MethodMap.tsx`）：
 * 泳道 = 方法家族，节点 = 方法（短名称 + 一句话贡献 + 少量策略标签），连线 = 真实关系。
 * 每个方法的「解决什么问题 / 核心做法 / 与相关方法的区别 / 原文依据」放在节点点击后的详情面板里，
 * 因此不再需要单独的卡片列表视图。
 *
 * 保留本文件仅为记录（本环境不允许删除文件）；它不再被任何界面引用。
 */
export {};
